<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Job Portal", root_url , id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home",   root_url %></li>
        <li><%= link_to "Help",   '#' %></li>
        <% if user_signed_in? %>
          <li><%= link_to('Logout', destroy_user_session_path, method: :delete) %> </li>
          <% if current_user.has_role? :employee %>

          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to('Create Job', new_job_path, method: :get) %> </li>
              <li class="divider"></li>
              <li><%= link_to('All My Jobs', jobs_path, method: :get) %></li>
              <!--<li class="divider"></li>
               <li><%= link_to('All Applied Users', applied_users_job_path(current_user.id)) %> </li>-->

            </ul>
          </li>
          <% else %>
              <li>
              <%= link_to('All Jobs', jobs_path, method: :get) %>
              </li>
          <% end %>
        <% else %>
         <li>
         <%= link_to('Login', new_user_session_path)  %>
         </li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>